{
  "eval_id": 3,
  "eval_name": "fastendpoints-vertical-slice",
  "prompt": "This repo uses FastEndpoints. Add a new Orders feature following the existing vertical-slice structure without converting anything to controllers.",
  "assertions": [
    "The response explicitly preserves vertical slices or feature-based organization.",
    "The response keeps FastEndpoints concepts such as request, response, endpoint, and validator together.",
    "The response does not recommend converting the feature to controllers or introducing unnecessary horizontal layers."
  ]
}
